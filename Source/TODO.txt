-- add phase distortion effect to FXProcessors via PDistort (with fifo/latency)
-- waveshaper OS and ADAA?

//======================

calculate latency from dnspl1+2

clean up Envelope:

bring back mod assign menus for envs on main page
related: hack Row to allow switching source or destination IN the MM?

box colors? headers?

lfo per-voice phase callbacks
lfo rand and S+H: recalc when safe?